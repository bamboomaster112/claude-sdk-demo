version: "3.8"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    depends_on: []

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_SUPABASE_URL=https://qnovdyhnxqxfuotygztd.supabase.co
      - VITE_SUPABASE_ANON_KEY=sb_publishable_ghfLBlzo1rOqKpbFodB0fg_jOdQBniQ
      - VITE_API_URL=http://localhost:8000
